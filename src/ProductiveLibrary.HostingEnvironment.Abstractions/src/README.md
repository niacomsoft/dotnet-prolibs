# README

> 提供了访问运行时主机环境的抽象方法集定义
>
> Provides an abstract method definitions for accessing the runtime host environment.

> [!NOTE]
>
> 此类库提供了一组访问运行时主机环境的抽象方法集。
>
> This library provide a set of abstract methods for accessing the runtime host environment.